# Audio Assets

This folder should contain the same audio files as `ui-vue/public/audio/`.

In development, Vite will serve files from here.
In the monorepo, these assets should be shared or symlinked.

For now, copy or symlink the audio files from `../ui-vue/public/audio/` here.
